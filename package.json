{
  "name": "agencia-byte-site",
  "version": "2.0.0",
  "private": true,
  "description": "Landing page Agencia Byte v2",
  "scripts": {
    "build": "esbuild entry.jsx --bundle --minify --inject:./react-shim.js --jsx=transform --define:process.env.NODE_ENV=\\\"production\\\" --outfile=app.js",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
